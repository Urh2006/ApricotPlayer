## What's new?

- the update window title is now `Update available`
- the update window now clearly shows `Version ...`, `What's new?`, and `Do you want to update now?`
- the buttons are now `Update now` and `Skip this version` instead of plain Yes/No
- choosing `Skip this version` remembers that release, so the same version is not offered again on every startup
