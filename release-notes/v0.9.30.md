# ApricotPlayer 0.9.30

## Modular Codebase Safety Review

- Added a stronger release-build preflight that checks all Apricot modules before packaging.
- Synchronized `apricot.__version__` with the app version.
- Hardened locale loading so missing newer keys inherit the English fallback at runtime.
- Rechecked MainFrame mixin method availability, module imports, locale coverage, settings rendering, dynamic results, direct links, local folder sorting, and the major navigable screens after the modular refactor.
