# What's new in version 0.6.10.6

- Fixed the Playback settings combo boxes so the visible first option matches the recommended default value.
- Pitch mode now shows `MPV (highest quality, recommended)` first.
- Speed audio mode now shows `Rubberband high quality (recommended)` first.
