# What's new in version 0.8.15

- Fixed the Default playback volume slider in Settings so screen readers hear the setting name and the actual 0-300 value clearly.
- Removed native slider tick labels from that control and reused ApricotPlayer's custom accessible slider reporting, matching the equalizer sliders more closely.
