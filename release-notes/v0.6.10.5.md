# What's new in version 0.6.10.5

- Fixed player play/pause after the video ends: pressing `Space` restarts from the beginning only when playback is still truly at the end.
- If you seek backward after a video finishes, `Space` now resumes from that current position instead of unexpectedly jumping back to the beginning.
