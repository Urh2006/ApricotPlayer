# ApricotPlayer 0.9.29

## Search and Results Stability Hotfix

- Fixed Enter in the Search query box so it reliably starts the search again.
- Fixed Enter in the Direct link field so it still uses the configured direct-link action.
- Hardened result row rendering for incomplete provider metadata, preventing partial results from breaking the list.
- Verified search result loading, dynamic result extension, keyboard interception, Python compilation, and runtime-risk static analysis after the modular refactor.
