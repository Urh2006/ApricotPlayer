## What's new in 0.3.3

- Pressing `Ctrl+Shift+A` or `Ctrl+Shift+D` now always speaks `Download started.` before the download begins.
- The completion popup setting is unchanged: when enabled, completion still uses a popup; when disabled, completion is spoken by the screen reader.
- Download startup is delayed slightly so very fast downloads do not immediately override the start announcement.
