## What's new in 0.3.8

- Settings now says `Check for updates at startup` instead of mentioning GitHub in the checkbox label.
- Added a manual `Check for updates` button in Settings.
- Manual update checks go directly to the latest GitHub release and ignore a previously skipped version, so users can retry an update immediately.
- Manual update checks announce the result for screen readers.
